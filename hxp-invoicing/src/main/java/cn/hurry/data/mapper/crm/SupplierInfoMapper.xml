<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.hurry.data.mapper.crm.SupplierInfoMapper">
	<resultMap id="SupplierResultMap" type="cn.hurry.po.crm.SupplierInfo">
		<id property="id" column="id" />
	</resultMap>
	
	 <insert id="insertSupplierInfo" parameterType="cn.hurry.po.crm.SupplierInfo">
	 	INSERT INTO supplier_info (name,phone,address,remark)values(#{name},#{phone},#{address},#{remark})
	 </insert>
	 
	 <delete id="deleteSupplierInfo" parameterType="cn.hurry.po.crm.SupplierInfo" >
	 	DELETE FROM supplier_info where id = #{id}
	 </delete>
	 
	 <update id="updateSupplierInfo" parameterType="cn.hurry.po.crm.SupplierInfo" >
	 	UPDATE supplier_info 
	 	<set>
	 		<if test="name != null">
	 			name = #{name},
	 		</if>
	 		<if test="phone != null">
	 			phone = #{phone},
	 		</if>
	 		<if test="address != null">
	 			address = #{address},
	 		</if>
	 		<if test="remark != null">
	 			remark = #{remark},
	 		</if>
	 	</set>
	 	where id = #{id}
	 </update>
	 
	 <select id="selectSupplierById" parameterType="int" resultMap="SupplierResultMap">
	 	SELECT * FROM supplier_info WHERE id = #{id}
	 </select>
	 
	 <sql id="where">
	 		<if test="name != null">
	 			AND name = #{name}
	 		</if>
	 		<if test="phone != null">
	 			AND phone = #{phone}
	 		</if>
	 		<if test="address != null">
	 			AND address =  #{address}
	 		</if>
	 		<if test="remark != null">
	 			AND remark = #{remark}
	 		</if>
	 </sql>
	 
	<select id="selectSupplierByMap" parameterType="map" resultMap="SupplierResultMap">
		SELECT * FROM supplier_info
		<where>
			<include refid="where"/>
		</where>
		<if test="sortField!=null and sortField!=''">
			ORDER BY ${sortField} ${sortOrder}
		</if>
		<if test="sortField==null or sortField==''">
			ORDER BY id DESC
		</if>
		<if test="firstResult != null">
			<if test="maxResults != null">
				LIMIT ${firstResult},${maxResults}
			</if>
		</if>
	</select>
	
	<select id="countSupplierByMap" parameterType="map" resultType="int">
		SELECT count(id) FROM supplier_info
		<where>
			<include refid="where"/>
		</where>
	</select>
</mapper> 